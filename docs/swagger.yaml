definitions:
  model.Book:
    properties:
      bookAuthor:
        type: string
      bookImageUrl:
        type: string
      bookIntro:
        type: string
      bookName:
        type: string
      id:
        type: integer
    type: object
  response.Response:
    properties:
      code:
        type: integer
      data:
        type: object
      msg:
        type: string
    type: object
info:
  contact: {}
  license: {}
paths:
  /book/list:
    get:
      consumes:
      - application/json
      description: 获取书籍列表
      parameters:
      - description: "1"
        in: path
        name: pageNum
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          headers:
            x-token:
              description: qwerty
              type: string
          schema:
            $ref: '#/definitions/model.Book'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/response.Response'
swagger: "2.0"
